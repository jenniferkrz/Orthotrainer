# Orthoruler
Embeddable widget for measuring distances On 3D scanned models with **ortho**graphic and perspective projection (it **rule**s). (**r**)

## Changing the model
Swap out the gltf file, ensure that the naming stays the same.
